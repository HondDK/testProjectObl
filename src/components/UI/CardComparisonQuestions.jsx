import React from "react";

const CardComparisonQuestions = (props) => {
	return (
		<div className="card_comparison_questions">
			<span>{props.children}</span>
		</div>
	);
};

export default CardComparisonQuestions;
